<!-- fw-report
round: 018-framework-3-0-0
role: verifier
branch: verifier/018-framework-3-0-0
head: a09e2e19bcdf8cdeb2d0ea3858166d9a8df78a7f
os: macOS 27.0
python: 3.9.6
written: 2026-09-23T11:00:06Z
-->
Reviewed commit: a09e2e19bcdf8cdeb2d0ea3858166d9a8df78a7f (origin/builder/018-framework-3-0-0). Commands I ran myself, with exit codes, all at this commit unless noted: `python3 tools/fw.py check` (0); `python3 -m unittest discover -s tests -v` under system Python 3.9.6 (1, one failure, 11 skips) and under `/opt/homebrew/bin/python3.13` (0, 115 run, 0 failures, 11 skips); `python3 tools/generate_messages.py --check` (0); `python3 tools/generate_protocol_constants.py --check` (0); `python3 tests/test_replay_trace.py --update` under 3.9.6 (1, `TypeError`) and under 3.13 (0) followed by `git diff --exit-code -- tests/golden` (0); `python3 <framework>/tools/adopt.py <project> --update --dry-run` (0, twice, identical plan both times); `python3 <framework>/tools/adopt.py <project> --update` for real (0, confirmed `docs/agents/framework.json` byte-identical before/after, tree left clean); `wc -w AGENTS.md docs/state.md` (1477, 629); `git diff --stat origin/master -- client data policy ui integration gframe ocgcore .github` (empty); `python3 -m unittest tests.test_push_guard -v` (0, 13/13); `python3 -m unittest tests.test_ci_required_checks -v` (0); `python3 -m unittest tests.test_framework -v` (0); `git grep` sweeps for 2.x-leftover terms and for personal paths/emails across tracked files; `gh api repos/cntrl-alt-lenny/edopro-next/branches/master --jq .protected` (0, returned `true`, run live today 2026-09-23); byte-diffs of every framework-installed file (`docs/agents/FRAMEWORK.md`, the three `docs/agents/roles/*.md`, `tools/fw.py`, all four `.claude/agents/*.md`+`.claude/commands/status.md`) against the pristine v3.0.0 framework/adapter sources (all identical); `git submodule update --init` and `git submodule status`.

Submodule status (after `git submodule update --init`, second attempt succeeded after a first network timeout):
```
 46779fbe40e6a9bd8967f5dc6a03f4eaa6550d57 ocgcore (v11.0-86-g46779fb)
```

## Findings

- [SHOULD FIX] `AGENTS.md:136-142` ("What is actually enforced") — the branch-protection claim still carries the same date, `2026-08-31`, as the pre-round `AGENTS.md` (verified identical wording/date via `git show origin/master:AGENTS.md`). Acceptance criterion 3's last bullet requires the entry-point file to "state which protections GitHub really enforces. Re-check them live and date the check." Nothing in `builder.md` shows a `gh api repos/.../branches/master --jq .protected` call actually run during round 018 (I searched the whole report for "protected"/"branch protection"/"gh api" and found only citations of the command, never an execution or its output), and the date was not bumped. I ran the exact command live today and it returned `true`, so the underlying claim is still correct and there is no live risk — but the round's own evidence for this specific criterion is missing, and the gap is not disclosed under the report's "Not verified" section either (which lists CI-at-commit, newer-framework-release, and Windows/macOS rebuilds, but not this). This is the same defect class (a stale, unre-verified branch-protection claim) the project's own docs say an external review once caught — worth a fix (re-run the live check and date it) even though nothing is currently false.

- [NOTE] `docs/rounds/018-framework-3-0-0/brief.md:257-260` (acceptance criterion 10) is worded so that it can never be literally satisfied: `adopt.py`'s `describe()` unconditionally appends a `record docs/agents/framework.json` line to every `--update` (dry-run or real) output, regardless of whether the manifest content changes — confirmed by reading `tools/adopt.py` and by running the real (non-dry-run) `--update`, which left `docs/agents/framework.json` byte-identical. The criterion says a clean second dry run must show "no … record … lines," which is impossible given the tool's actual behaviour. `docs/rounds/018-framework-3-0-0/builder.md:566-568` quotes its own dry-run output (which contains a `record` line) and then asserts "matching acceptance criterion 10 exactly" without acknowledging the discrepancy. The substance is fine (nothing was actually left to do), but the claim of an exact literal match is not accurate, and the brief's wording itself should be corrected for any future adopter round.

- [NOTE] `docs/BASELINE.md:46` still contains a personal WSL path, `/mnt/c/Users/leona/Dev/edopro-next`, unchanged since before this round (confirmed via `git diff origin/master..HEAD -- docs/BASELINE.md`, empty). It predates round 018, is outside this round's declared scope, and is not caught by `fw.py check`'s personal-path scan because `_live_docs()` in `tools/fw.py` only scans `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, `docs/state.md`, `docs/agents/**/*.md` and `docs/rounds/*/*.md` — `docs/BASELINE.md` is not in that set. Not a regression, but the round's required "inventory of other 2.x leftovers" (which explicitly asked for "anything else you find" against the "nothing personal in any tracked document" invariant) did not surface it. Worth a follow-up round.

- [NOTE, confirms builder's own disclosure] `tests/test_readme_status.py::CommandLineTest::test_check_fails_on_a_stale_copy_and_update_repairs_it` fails under this machine's system Python (3.9.6) with `TypeError: write_text() got an unexpected keyword argument 'newline'` (the `newline=` kwarg needs Python ≥3.10). I reproduced this independently, and also reproduced the identical failure on an unmodified `origin/master` checkout via `git worktree`, proving it predates this round and is not caused by its diff (`tools/generate_readme_status.py` has no diff between `origin/master` and the reviewed commit). Under `/opt/homebrew/bin/python3.13` the full suite is clean (115 run, 0 failures, 11 skips, all skips either C++-binary-dependent or Windows-ACL-dependent). `builder.md` discloses exactly this, with the same root cause and the same cross-check method — independently reproduced and confirmed accurate.

- No BLOCKER found among the round's actual file changes. Every framework-installed file I diffed byte-for-byte against the pristine v3.0.0 release/adapter sources came back identical; every 2.x file the brief required gone (`docs/agents/{CONSTITUTION,adapters,evidence,reports,topologies,git-and-isolation,model-notes,launching,worktree-mechanism}.md`, `docs/agents/roles/README.md`, `docs/roles/`, `.claude/agents/builder.md`, `.claude/README.md`, `.claude/settings.json`, `.claude/hooks/`, `docs/briefs/active.md`, `tools/checkout.py`, `tools/report.py`, `tests/test_docs_consistency.py`, `tests/test_save_agent_reply.py`, `tests/test_checkout.py`, `tests/test_report.py`) is in fact gone; `docs/briefs/archive/016-2026-09-22-records-and-test-gaps.md` is byte-identical to `origin/meta/presentation-tidy`'s copy; `AGENTS.md` (1477 words), `CLAUDE.md` (pure `@AGENTS.md` pointer) and `docs/state.md` (629 words) meet every literal, checkable sub-bullet of acceptance criteria 3-5 that I could independently verify except the branch-protection re-check above; the push guard (13/13), the five CI required-check names, and the golden traces (byte-for-byte reproduction) are all unchanged in substance; `git diff --stat origin/master -- client data policy ui integration gframe ocgcore .github` is empty, so no product code, `gframe/`, `ocgcore` pointer, or CI workflow was touched. The rule-accounting table's word counts and byte counts for the old `CLAUDE.md` (7,717 bytes), old `AGENTS.md` (3,407 words) and old `docs/state.md` (3,555 words) are all exactly correct, which I independently confirmed.

## Not verified

- C++ modules (`client/`, `data/`, `policy/`, `ui/`): not built or tested, matching the brief's explicit "C++ modules are out of scope and need not be built."
- Whether a newer framework release than 3.0.0 now exists — my `fw.py status` run did reach the network today and reported "up to date with the latest release (3.0.0)," which is one data point more than the builder had (their run timed out), but I did not independently query the framework repository's release list by hand.
- Windows and macOS CTest/build behaviour post-adoption: this round touched no build files, and I did not attempt a build on either platform.
- CI at the reviewed commit: the branch was pushed by `fw.py start`'s prerequisite delivery, but I did not check a live CI run for this exact SHA.
- Whether `docs/agents/local/windows-notes.md`'s content is complete relative to every Windows fact the old `AGENTS.md` held — I spot-checked it against the quoted old text and found it a faithful copy, but did not diff every character.

## Verdict

This round does what the brief and the 3.0.0 CHANGELOG's adopter steps ask, and the mechanical, checkable acceptance criteria I could independently reproduce all hold: every installed framework file is byte-identical to the pristine v3.0.0 release, every named 2.x file is actually gone, the entry-point documents (`AGENTS.md`, `CLAUDE.md`, `docs/state.md`) are within their word budgets and hold the required content, the push guard and CI-required-check names are provably unchanged, no product code or `gframe/`/`ocgcore` was touched, and the round's own evidentiary claims (byte/word counts, the pre-existing Python 3.9 test failure, the golden-reproduction result) all reproduced exactly as reported when I checked them independently — this is a well-evidenced, carefully self-checked piece of work. The one real gap is that the branch-protection claim in the new `AGENTS.md` was not shown to have been re-verified live during this round, only carried forward with its old date; I confirmed the claim is still true today by running the check myself, so there is no live risk in merging, but the round's own report does not close this specific, explicitly-named acceptance-criterion bullet, and does not flag it as unverified either. The other two findings (the acceptance criterion 10 wording being literally unsatisfiable by `adopt.py`'s actual behaviour, and one pre-existing, out-of-scope personal path in `docs/BASELINE.md`) are minor and do not touch this round's own diff. I am confident in this verdict: I independently reproduced essentially every checkable claim in the round's report rather than relying on its narrative.
