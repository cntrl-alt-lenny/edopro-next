# Retired path

Builder uses the canonical Worker contract at
[`../agents/roles/worker.md`](../agents/roles/worker.md), as declared in
`AGENTS.md`. This compatibility file is not a second Builder contract.
