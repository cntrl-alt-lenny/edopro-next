# Retired role-contract location

The canonical, provider-neutral role contracts now live under
[`../agents/roles/`](../agents/roles/). This directory is retained only so
historical links resolve; it is not a normative source.
